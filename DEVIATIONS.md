# DEVIATIONS

None yet.

## Notes (not deviations)
- Tailwind v4 (CSS-first config via `@theme` in `src/styles/globals.css`) is used instead of a
  `tailwind.config.js` file. The brief specifies "Tailwind CSS" without a version; v4 is current
  and reduces config surface. All brief colour tokens are defined as theme variables.
